apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: EdgeRunner
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: LinuxEc2

    steps:
      - name: Say hello
        shell: sh
        run: |
          echo "hello world"
